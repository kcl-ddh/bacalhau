#!/usr/bin/env python

# -*- coding: utf-8 -*-

from bacalhau.corpus_manager import CorpusManager
from bacalhau.data.tei_manager import TEIManager

CORPUS_PATH = 'corpus'

if __name__ == "__main__":
    cm = CorpusManager(CORPUS_PATH, TEIManager)
    cm.extract()
    print(cm._corpus.words())
